const CONFIG = {
    api : {
        endpoint: 'http://client.fr/api/'
    },
    app: {
        sessionExpirationTimeInSeconds: 3600,
        logDebugEnabled: false,
        preferredLanguage: 'fr_FR'
    }
};